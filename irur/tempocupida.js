// Assuming jQuery is already loaded and document is ready
$(function() {
  // Cache the modal element for future use
  var $modal1 = $('#modal1');
  
  // Close the modal when needed
  $modal1.modal('close');
});
